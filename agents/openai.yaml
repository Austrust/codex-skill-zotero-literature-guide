interface:
  display_name: "Zotero Literature Guide"
  short_description: "Chinese Zotero guides with plugin write-back"
  default_prompt: "Use $zotero-literature-guide to build a Chinese literature guide package and attach it back through the Zotero Guide Helper plugin when confirmed."

policy:
  allow_implicit_invocation: true
